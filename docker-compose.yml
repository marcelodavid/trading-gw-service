services:
  trading-gateway:
    platform: linux/amd64
    build:
      context: .
      target: development
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env
    command: uvicorn app.entrypoints.fastapi.main:app --host 0.0.0.0 --port 8000 --reload
